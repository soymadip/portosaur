{
  "name": "{{repo_name}}",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "$npm_execpath install && porto dev",
    "build": "$npm_execpath install && porto build",
    "serve": "porto serve"
  },
  "dependencies": {
    "@portosaur/cli": "{{porto_version}}",
    "@portosaur/theme": "{{porto_version}}"
  }
}
