{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "$npm_execpath install && porto dev",
    "build": "$npm_execpath install && porto build",
    "serve": "porto serve"
  },
  "dependencies": {
    "@portosaur/cli": "{{portoVersion}}",
    "@portosaur/theme": "{{portoVersion}}"
  }
}
